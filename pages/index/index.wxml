<!--index.wxml-->

<swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" class="banners" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{swiper_url}}">
    <swiper-item class="banner">
      <image src="{{item}}" bindtap="bindViewTap" class="banner-image" width="100%" height="100%" />
      <text class="banner-title">{{item.title}}</text>
    </swiper-item>
  </block>
</swiper>
<view wx:if="{{check == true}}">
  <navigator url='../edit/edit'>
    <image class="append" src="../../images/source/append.png"></image>
  </navigator>
</view>
<view class="swiper-tab">
  <button class="category" bindtap="actionSheetTap">{{cur_type}}▾</button>
  <action-sheet hidden="{{actionSheetHidden}}" bindchange="actionSheetChange">
    <block wx:for-items="{{actionSheetItems}}">
      <action-sheet-item class="item" bindtap="bind{{item}}">{{item}}</action-sheet-item>
    </block>
    <action-sheet-cancel class="cancel">取消</action-sheet-cancel>
  </action-sheet>
  <view class="swiper-tab-item {{activeIndex==0?'active':''}}" bindtap="stateswitch" data-index="0">LOST</view>
  <view class="swiper-tab-item {{activeIndex==1?'active':''}}" bindtap="stateswitch" data-index="1">FOUND</view>
</view>
<icon type='search' bindtap='search' class="refresh"></icon>
<view class="news-item-container">
  <!-- <block wx:for="{{listofitem}}" wx:for-index="id">
    <text wx:if="{{item.header}}" class="sub-title">{{item.header}}</text>
    <view wx:else class="news-item">
      <view class='userinfo'>
        <navigator url="../userinfo/userinfo?userid={{item.userid}}">
          <image src="{{item.usericon}}" class='icon-image'></image>
        </navigator>
        <view class='username'>
          <navigator url="../userinfo/userinfo?userid={{item.userid}}">
            <text selectable='true'>{{item.username}}</text>
          </navigator>
        </view>
      </view>
      <view class="news-item-left">
        <text selectable='true' class="news-item-title">{{item.text}}</text>
      </view>
      <view wx:if="{{item.image != ''}}" class="image-container">
        <block wx:for="{{item.imagelist}}" wx:for-item="specific_url">
          <image bindtap="photopreview" data-list="{{item.imagelist}}" src="{{specific_url}}" data-src="{{specific_url}}" class="news-image" mode="aspectFill">
          </image>
        </block>
      </view>
      <view class="msginfo">
        <text class='time'>{{item.sub_time}}</text>
        <view class="address">{{item.address}}</view>
      </view>
    </view>
  </block> -->
  <block wx:for="{{listofitem}}" wx:for-index="id">
    <text wx:if="{{item.header}}" class="sub-title"></text>
    <view wx:else class="news-item-cls">
      <view class='userinfo-cls'>
        <navigator url="../userinfo/userinfo?userid={{item.userid}}">
          <image src="{{item.usericon}}" class='icon-image-cls'></image>
        </navigator>
        <view class='username-cls'>
          <navigator url="../userinfo/userinfo?userid={{item.userid}}">
            <text selectable='true'>{{item.username}}</text>
          </navigator>
        </view>
        <view class='apply-view-cls'>申请</view>
      </view>
      <view class="news-card-cls">
        <view class='news-info-cls'>
          <view class='news-info-desc-cls'>
            <text class="news-item-title-cls"> <text style="color:gray"> 拾取物件：</text>物件名字</text>
          </view>
          <view class='news-info-desc-cls'>
            <text class="news-item-title-cls"><text style="color:gray"> 拾取地点：</text>{{item.address}}</text>
          </view>
          <view class='news-info-desc-cls'>
            <text class='time-cls'>{{item.sub_time}}</text>
          </view>
        </view>
        <view class='new-image-container-cls'>
          <image src="{{item.usericon}}" class='news-img-cls'></image>
          <view class='tag-cls'>
            <text>{{item.type}}</text>
          </view>
        </view>
        <!-- </navigator> -->
      </view>
      <!-- <view wx:if="{{item.image != ''}}" class="image-container">
        <block wx:for="{{item.imagelist}}" wx:for-item="specific_url">
          <image bindtap="photopreview" data-list="{{item.imagelist}}" src="{{specific_url}}" data-src="{{specific_url}}" class="news-image" mode="aspectFill">
          </image>
        </block>
      </view> -->
      <!-- <view class="msginfo">
        <text class='time'>{{item.sub_time}}item.sub_time</text>
        <view class="address">{{item.address}}item.address</view>
      </view> -->
    </view>
  </block>
</view>